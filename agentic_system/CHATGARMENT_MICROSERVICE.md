# ChatGarment 독립 서비스 (마이크로서비스) 구조

## ✅ 현재 상태 분석

### Fashion Agentic AI System 완료 상태

- ✅ **Agent Runtime (Agent 1)** - 완료
- ✅ **F.LLM (Agent 2)** - InternVL2 8B 통합 완료
- ✅ **Custom UI** - 완료
- ✅ **Memory Manager** - 완료
- ✅ **RAG Store** - Vector DB 통합 완료
- ✅ **Frontend** - React 3D 뷰어 완료
- ✅ **API Server** - FastAPI 완료
- ⚠️ **ChatGarment 통합** - 코드 완료, 실제 실행은 리눅스 환경 필요

### ChatGarment 특성

1. **리소스 집약적**
   - 대용량 GPU 메모리 필요 (16GB+)
   - 모델 추론 시간 소요 (수 초~수십 초)
   - 리눅스 환경 권장

2. **독립 실행 가능**
   - 이미지 입력 → JSON 출력 (자체 완결)
   - GarmentCodeRC 연동 (별도 프로세스)

3. **확장성 고려**
   - 여러 GPU 서버로 분산 가능
   - 배치 처리 가능
   - 캐싱 최적화 가능

## 🏗️ 마이크로서비스 아키텍처 제안

### 권장 구조

```
┌─────────────────────────────────────────────────┐
│     Fashion Agentic AI System (Main)            │
│                                                 │
│  ┌─────────────┐      ┌──────────────┐        │
│  │ Custom UI   │      │ API Server   │        │
│  │ (Frontend)  │──────│ (FastAPI)    │        │
│  └─────────────┘      └──────┬───────┘        │
│                               │                │
│                      ┌────────┴────────┐      │
│                      │ Agent Runtime   │      │
│                      │ (Agent 1)       │      │
│                      └────────┬────────┘      │
│                               │                │
│              ┌────────────────┼────────────────┐
│              │                │                │
│       ┌──────▼──────┐  ┌─────▼─────┐  ┌─────▼─────┐
│       │ F.LLM       │  │ RAG Store  │  │ Functions │
│       │ (Agent 2)   │  │            │  │           │
│       └─────────────┘  └────────────┘  └───────────┘
└─────────────────────────────────────────────────┘
              │
              │ HTTP API 호출
              ▼
┌─────────────────────────────────────────────────┐
│   ChatGarment Service (독립 서비스)             │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │ ChatGarment API Server                  │  │
│  │ (FastAPI / gRPC)                        │  │
│  └─────────────┬───────────────────────────┘  │
│                │                                │
│  ┌─────────────▼───────────────────────────┐  │
│  │ ChatGarment Pipeline                    │  │
│  │ - VLM 추론                              │  │
│  │ - GarmentCode 패턴 생성                 │  │
│  │ - GarmentCodeRC 3D 변환                 │  │
│  └─────────────────────────────────────────┘  │
│                                                 │
│  리눅스 환경 / GPU 서버                         │
└─────────────────────────────────────────────────┘
```

## ✅ 독립 환경으로 분리하는 것이 좋은 이유

### 1. **리소스 격리**
- GPU 메모리 집약적 작업 분리
- 메인 서버 영향 최소화
- 독립적 확장 가능

### 2. **배포 유연성**
- 리눅스 환경에서만 ChatGarment 실행
- 메인 시스템은 다른 환경에서도 실행 가능
- 마이크로서비스 아키텍처 패턴

### 3. **확장성**
- 여러 ChatGarment 서버로 분산
- 부하 분산 가능
- 배치 처리 전용 서버 구성 가능

### 4. **유지보수**
- 독립적 업데이트
- 버전 관리 분리
- 장애 격리

### 5. **현재 코드 구조 호환**
- 이미 `extensions.py`에서 분리 가능한 구조
- API 호출로 쉽게 변경 가능

## 🔧 구현 방안

### 옵션 1: HTTP API 서비스 (권장)

#### ChatGarment 서비스 구성

```python
# chatgarment_service/main.py
from fastapi import FastAPI, File, UploadFile
from agentic_system.tools.chatgarment_integration import ChatGarmentPipeline

app = FastAPI(title="ChatGarment Service")

pipeline = ChatGarmentPipeline(device="cuda")
pipeline.load_model()

@app.post("/api/v1/process")
async def process_image(image: UploadFile):
    """이미지를 처리하여 3D 의류 생성"""
    # 이미지 저장
    image_path = save_uploaded_file(image)
    
    # ChatGarment 파이프라인 실행
    result = pipeline.process_image_to_garment(
        image_path=image_path,
        garment_id=f"job_{uuid.uuid4()}"
    )
    
    return result
```

#### 메인 시스템에서 호출

```python
# agentic_system/tools/extensions.py
import requests

def extensions_2d_to_3d_tool(parameters, context):
    """ChatGarment 서비스 호출"""
    image_path = parameters.get("image_path")
    
    # 독립 서비스로 요청 전송
    with open(image_path, 'rb') as f:
        response = requests.post(
            "http://chatgarment-service:8001/api/v1/process",
            files={"image": f}
        )
    
    return response.json()
```

### 옵션 2: 현재 구조 유지 + 환경 분리

현재 코드 구조를 유지하되, ChatGarment만 리눅스 환경에서 실행:

```python
# agentic_system/tools/extensions.py
class Extensions2DTo3D:
    def __init__(self):
        # ChatGarment 파이프라인 초기화
        self.chatgarment_pipeline = ChatGarmentPipeline(device="cuda")
        
        # 리눅스 환경에서만 로딩
        if self._is_linux():
            self.chatgarment_pipeline.load_model()
        else:
            # 다른 환경에서는 API 호출
            self.use_api = True
```

## 📋 독립 환경 실행 체크리스트

### ChatGarment 독립 서비스 설정

1. **환경 분리**
   - [ ] 리눅스 서버 설정 (GPU 권장)
   - [ ] Python 가상환경 구성
   - [ ] ChatGarment 의존성 설치
   - [ ] 모델 체크포인트 준비

2. **서비스 구성**
   - [ ] ChatGarment API 서버 구현
   - [ ] Docker 컨테이너화 (선택사항)
   - [ ] 서비스 시작 스크립트
   - [ ] 헬스체크 엔드포인트

3. **통합**
   - [ ] 메인 시스템에서 API 호출 코드
   - [ ] 에러 처리 및 재시도 로직
   - [ ] 타임아웃 설정
   - [ ] 로깅 및 모니터링

## 🚀 실행 방법

### ChatGarment 독립 서비스 실행 (리눅스)

```bash
# ChatGarment 서비스 디렉토리
cd chatgarment_service

# 가상환경 활성화
source venv/bin/activate

# 서비스 시작
uvicorn main:app --host 0.0.0.0 --port 8001
```

### 메인 시스템 실행 (어떤 환경이든)

```bash
# 메인 시스템 디렉토리
cd agentic_system

# API 서버 시작
uvicorn api.main:app --host 0.0.0.0 --port 8000
```

## ✨ 권장사항

### ✅ 독립 환경으로 진행 권장

**이유:**
1. **리소스 효율성**: GPU 집약적 작업 분리
2. **확장성**: 여러 서버로 분산 가능
3. **유지보수**: 독립적 업데이트 및 관리
4. **현재 코드 구조**: 이미 분리 가능한 구조

### 🔄 마이그레이션 전략

1. **1단계**: 현재 구조 유지, ChatGarment만 리눅스에서 실행
2. **2단계**: ChatGarment를 독립 API 서비스로 분리
3. **3단계**: 마이크로서비스 아키텍처로 완전 분리

## 📝 결론

**✅ ChatGarment를 리눅스 환경에서 독립적으로 진행하는 것을 강력히 권장합니다.**

이유:
- Fashion Agentic AI System은 이미 완성되어 있음
- ChatGarment는 리소스 집약적이므로 독립 실행이 적합
- 마이크로서비스 패턴으로 확장성 확보
- 현재 코드 구조에서 쉽게 분리 가능

**구현 방법:**
- ChatGarment를 독립 FastAPI 서비스로 구현
- 메인 시스템에서 HTTP API로 호출
- 환경 변수로 서비스 URL 설정

이렇게 하면 메인 시스템은 어떤 환경에서든 실행 가능하고, ChatGarment만 리눅스 GPU 환경에서 독립적으로 실행할 수 있습니다.

